#include <stdio.h>

int main(void)
{
  unsigned char i;
  unsigned long long total = 0;

  for(i = 1; i >= 1; i++) {
    total++;
  }

  printf("%llu execuções para a volta completa!", total);

  return 0;
}